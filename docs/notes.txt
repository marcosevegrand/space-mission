Uma das missões do rover poderia ser "capturar imagens", para isso seria necessário no nosso pacote
mission update ter um field para armazenar as imagens que penso eu serão serializadas para isto ser possível
logo no mission update teriamos de ter um field data []byte para conter os dados gerados
assim sendo teriamos de ter um codec para esses dados, sem se quer me estar a referir ao codec necessário para
serializar os dados para enviar na rede


O que falta:
- in-order delivery
- mission failure on max duration timeout (in progress)
- improved memory management and mutex usage in udp link (in progress)
- improve logging capabilities and fix excess logging in udp link
- implement observation api (HUGO E FRANCISCO ;-;)



-----------------
------ Q1 -------
No missionLink já temos:
    Acknowledgements
    Retransmissions
    Error Detection
Também precisamos de implementar:
    Error Correction (no caso FEC)
    In-Order Delivery

-----------------
------ Q2 -------
Na simulação do rover eu tenho um buffer (que é uma slice de strings) que guarda os dados que são gerados entre
o último e próximo Mission Update
Quando chegar o momento de fazer o update é preferível/suposto que seja mandado um único pacote Mission Update
com todos os dados obtidos ou é também uma possibilidade enviar um burst de pacotes MissionUpdate
No nosso caso é levantada essa questão porque o dados buferizados entre updates são separados por momentos de geração
por exemplo o rover com um updateFrequency de 3 segundos:
t = 0
    recebe um mission assignment
    envia um update a dizer que a mission está em progress
t = 3s
    consulta o buffer e vê lá guardados os dados:
    ["Dados obtidos no intervalo [0,1]", "Dados obtidos no intervalo [1,2]", "Dados obtidos no intervalo [2,3]"]
        (isto porque o sensor que fornece estes dados, fornece-os de segundo em segundo)
    o rover agora dependendo de como decidirmos pode concatenar este dados todos e envia-los num unico pacote Mission Update ou simplesmente mandar um MissionUpdate por string nesse buffer, neste caso mandar 3 mission updates

Exemplo:
Eu obtenho dados ambientais dos sensores de 2 em 2 segundos mas o Mission Update Frequency é de 10 segundos
Entre cada update são obtidos dados ambientais distintos 5 vezes, no momento de enviar o update mandamos 5 pacotes MissionUpdate cada um com um conjunto de dados ambientais ou deve concatenar os 5 conjuntos de dados e enviar num único pacote ou ainda deve processar esses 5 conjuntos e fazer uma média do dados no intervalo? Qual é o preferível?

-----------------
------ Q3 -------
No receive loop do peer UDP que escuta por pacotes, é criada uma thread para o processamento de cada pacote recebido
caso o peer seja inundado com pacotes isto pode fazer com sejam criadas um número excessivo de threads, está tudo ok
porque no nosso cenário os rovers e a nave mãe não chegariam a esse ponte ou seria melhor criar uma pool de threads por
precaução?

=======================================================================
=======================================================================
=======================================================================
=======================================================================

// Rover      -- Request    --> Mothership
// Mothership -- Assignment --> Rover
// Rover      -- Update     --> Mothership
Para atribuir uma missão ao rover, seria pertinente atribuir uma missão próxima à localização do rover.
Podemos/devemos basear-nos na posição que é transmitida na telemetria ou deveria o próprio protocolo do MissionLink passar essa informação no MissionRequest?
>>> podemos usar a posição transmitida na telemetria

Quem é que deve impor o limite de duração da missão? Deve a mothership mandar o rover parar quando passar do limite ou deve o rover dar a missão por terminada quando passar do limite?
>>> mothership

Os timeouts podem ser hardcoded ou é preferível serem configuráveis pelo caller dos modulos de transporte?
>>> podemos usar timeouts hardcoded no binário final

No conceito de passagem de tempo, se fizermos o tempo da simulação ser uma fração do tempo real (por exemplo 1 minuto corresponde a um ciclo dia/noite na simulação) para simular o ciclo do dia e características ambientais do planeta temos problemas em que a timestamp da telemetry e das missões precisam de ser definidas com base no tempo da simulação e depois temos outras variáveis como timeouts e intervalos de atualização (por exemplo envio de telemetria e de updates de missão) que ficaram confusos porque seriam definidos em termos de tempo real e não da simulação em alguns casos. Basicamente uma confusão entre o tempo da simulação e o time.Time (tempo real).
>>> podemos fazer uma simulação mais simples

No protocolo UDP devemos forçar que um caller de um send espere pelo ack antes de poder avançar ou é melhor disponibilizar um canal caso ele queira esperar para garantir que o pacote foi recebido com sucesso, mas também ter a possibilidade de continuar depois de fazer send.
>>> nao deve ser forçado a esperar

O Handle do payload deve ser passado a uma goroutine para que seja possível continuar a receber pacotes ou deve se esperar que um payload seja processado antes de aceitar o próximo pacote.
>>> tanto faz

===============================================================
===============================================================
===============================================================
===============================================================


o progresso que o rover envia à mothership pelo mission link basta ser algo q diz "50%" foi concluido ou tem de devolver informação/dados relativos à missão em questão

é preciso usar mutex lock no telemetry stream

devo implementar o tcp server de forma modular sem referencias ao telemetry stream ou podem estar juntos


===========


1. Relativamente aos Codecs, eu tenho nas minhas structs campos como MissionStatus (mission.go) e OperationalState (rover/telemetry, common.go) que são enums

> Exemplo:
        // OperationalState represents the current operational mode of a rover.
        type OperationalState uint8

        // Operational states that a rover can be in.
        const (
       	StateIdle      OperationalState = iota + 1 // Rover is stationary and idle
       	StateMoving                                // Rover is in motion
       	StateOnMission                             // Rover is executing a mission
       	StateError                                 // Rover encountered an error
       	StateUnknown                               // Rover state is unknown
        )

        func (os OperationalState) String() string {
       	return [...]string{"idle", "moving", "onMission", "Error", "Unknown"}[os-1]
        }

Desta forma os codecs acabam por nunca ter de serializar strings o que diminui o tamanho do payload, mas tenho receio
que ao não demostrar que consigo escrever um serializer que serializa dados de tamanho variável podemos perder pontos
ESTÁ TRANQUILO, ESTÁ FAVORÁVEL

2. Ao implementar o UDP link, o Go fornece duas formas de trabalhar com os sockets, listen e dial que permitem criar uma relação servidor-cliente. No meu caso, a minha implementação do servidor e cliente só se distingue pelo facto de um usar listen (e portanto WriteToUDP/ReadFromUDP) e o outro dial (e portanto Write/Read). Eu acho que seria mais simples usar Listen para ambos e ter o mesmo código para o servidor e cliente que no caso acho que seriam peers.
PROVAVELMENTE PREFERIVEL USAR SÓ LISTEN

3. Para o TCP stream seria mais adequado implementa-lo de forma a futuramente poder ser usado para comunicação bidirecional ou basta que tenha a capacidade dos clientes fazerem streaming de telemetry para o servidor.
NAO É NECESSÁRIO SE JUSTIFICADO

4. Os clientes TCP devem ter a capacidade de auto-reconnect ao servidor caso a ligação por algum motivo seja perdida?
FAZER
